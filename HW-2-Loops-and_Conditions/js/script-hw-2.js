const minNum = Number(prompt("Введіть число з якого будемо починати розрахунок"));
const maxNum = Number(prompt("Введіть кінцеве число розрахунку"));

if (Number.isInteger(minNum)) {    
}else{
    minNum = prompt("Введіть інше число");
}

if (Number.isInteger(maxNum)) {    
}else{
    maxNum = prompt("Введіть інше число");
}

